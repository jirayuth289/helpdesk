export type REWARDS_TYPE = {
    shopId?: string,
    rewardName: string,
    image?: string,
    point: number,
    description?: string,
    status?: string,
    couponExpDays: number,
    rewardLimit: number,
}